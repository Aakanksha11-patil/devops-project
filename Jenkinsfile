pipeline {
    agent any

    stages {

        stage('Hello') {
            steps {
                sh 'echo Jenkins Pipeline Working'
            }
        }

        stage('System Info') {
            steps {
                sh 'uname -a'
            }
        }

    }
}